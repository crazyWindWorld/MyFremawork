# 2026-05-04

## 全量重写游戏服务器文件

用户反映代码"不见了"（实际原因：IDE 打开的是旧的 TestTcpServer.csproj 而非 GameServer.csproj），对所有 19 个文件进行了完整重写：

- 修复了上版本 PingHandler 中的格式化字符串语法错误（`:o` 不合法）
- 所有文件内容均已验证行数非零，写入成功
- 根本问题：两个 .csproj 共存于同一目录，需用 GameServer.csproj 打开

### 文件清单（19个）
Core/Net/GameSession.cs, GatewayServer.cs
Core/Context/PlayerStateCache.cs
Core/Protocol/Packet.cs, FrameCodec.cs
Core/Routing/MessageRouter.cs, HandlerBase.cs, MsgHandlerAttribute.cs, HandlerServiceExtensions.cs
Data/Abstractions/IEntity.cs, IRepository.cs, IUnitOfWork.cs
Data/InMemory/InMemoryRepository.cs, InMemoryUnitOfWork.cs, InMemoryDataExtensions.cs
Handlers/LoginHandler.cs, PingHandler.cs, LogoutHandler.cs
Services/HeartbeatService.cs, GatewayHostedService.cs
Program.cs, ProtoCmds.cs, appsettings.json, appsettings.Development.json

## 修复"文件在解决方案中不显示"问题（2026-05-04 晚）

- **根本原因**：没有 .sln 文件，VS 直接打开 TestTcpServer.csproj（旧项目，手动 Compile 列表，不含新文件）
- **修复内容**：
  1. 创建 `E:\UnityProject\MyFremawork\Server\C#Server\TestTcpServer\GameServer.sln`，包含 GameServer + TestTcpServer 两个项目
  2. 补写缺失的 `Program.cs`（根目录）
  3. `LoginHandler.HashPassword` 改为 `internal static`（供 Program.cs 种子数据调用）
  4. 删除 GameServer.csproj 中冗余的 `<Compile Include="Proto\**\*.cs" />`（SDK 风格自动包含，手动添加会导致 IDE 显示异常）
- **给用户的操作**：用 Visual Studio 打开 `GameServer.sln`，将 GameServer 设为启动项目即可看到所有新文件
- **NuGet 编译问题**：命令行 `dotnet build` 报 `path1 null` 错误，属于当前 Windows Server 环境 NuGet 路径解析问题，与代码无关；建议在 VS 内直接编译

## 修复"重复 Compile 项"警告（2026-05-04 晚 23:00）

- **报错**：SDK 默认自动包含所有 .cs 文件，但 VS 缓存了旧版 GameServer.csproj 中的 `<Compile Include="Proto\**\*.cs" />`，导致重复
- **修复**：
  1. GameServer.csproj 里已无 `<Compile>` 项（之前已删除），问题出在 obj/ 缓存
  2. 清理了 `obj/` 和 `bin/` 目录
  3. 从 GameServer.sln 中移除了已不存在的 TestTcpServer.csproj 项目引用
- **用户操作**：在 VS 中关闭解决方案 → 重新打开 `GameServer.sln`，让 VS 重新解析项目

## 修复 ProtoCmds 命名不匹配（2026-05-04 23:05）

- **报错**：`ProtoCmds` 未包含 `PingReq` 的定义
- **原因**：`ProtoCmds.cs` 是自动生成的，心跳命令叫 `PING`/`PONG`（4000/4001），不含 `PingReq`/`PongRsp`/`LogoutReq`/`LogoutRsp`/`KickPush`
- **修复**：
  1. 在 `ProtoCmds.cs` 尾部追加 GameServer 新增命令：`PingReq = PING`(4000)、`PongRsp = PONG`(4001)、`LogoutReq`(10008)、`LogoutRsp`(10009)、`KickPush`(10012)
  2. `PlayerStateCache.TryKickAsync` 中 `ProtoCmds.LogoutPush` 改为 `ProtoCmds.KickPush`（踢人推送用专用 ID）
- **注意**：`ProtoCmds.cs` 标记为 Auto-generated，追加的新命令放在文件末尾注释区分
